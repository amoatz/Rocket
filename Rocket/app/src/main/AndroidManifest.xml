<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rocket">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_r_white_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_r_white_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/Theme.Rocket">
        <activity
            android:name=".activities.QuestionsLocationRatingActivity"
            android:exported="true" />
        <activity
            android:name=".activities.EnterCodeActivity"
            android:exported="true" />
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.CommentsActivity"
            android:exported="true" />
        <activity
            android:name=".activities.FilterActivity"
            android:exported="true" />
        <activity
            android:name=".activities.LocationRatingActivity"
            android:exported="true" />
        <activity
            android:name=".activities.RegisterActivity"
            android:exported="true" />
        <activity
            android:name=".activities.SignInActivity"
            android:exported="true" />
        <activity
            android:name=".activities.LocationActivity"
            android:exported="true" /> <!-- android:parentActivityName=".activities.SearchActivity" -->
        <!-- <meta-data -->
        <!-- android:name="android:support.PARENT-Activity" -->
        <!-- android:value=".activities.SearchActivity"/> -->
        <activity
            android:name=".activities.SearchActivity"
            android:exported="true"
            android:label="Where can I eat?"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true" />
    </application>

</manifest>